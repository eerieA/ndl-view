// Input your dev API key and then copy and rename as `runtime-config.js`
// This is for local dev. Angular's dev server will load it because
// we added src/assets in architect.build.assets in angular.json
window.NDL_API_KEY = "your-dev-api-key-here";